import { useEffect,useState } from 'react';
import { Link,Navigate,useParams } from 'react-router-dom';
import { ArrowLeft,CalendarDays,ChevronRight,Clock3,Newspaper,Radio,Trophy } from 'lucide-react';
import { apiFetch } from '../api';
import { demoArticles,findCompetition } from '../data/content';
const live=['LIVE','1H','HT','2H','ET'],done=['FT','AET','PEN'];const isLive=m=>live.includes(String(m.status).toUpperCase()),isFinished=m=>done.includes(String(m.status).toUpperCase());
export default function CompetitionPage({lang='en'}){const{slug}=useParams();const fallback=findCompetition(slug);const[data,setData]=useState(fallback?{competition:fallback,matches:[],standings:[],demo:true}:null);const[tab,setTab]=useState('overview');const[loading,setLoading]=useState(Boolean(fallback));useEffect(()=>{if(!fallback)return;setData({competition:fallback,matches:[],standings:[],demo:true});setLoading(true);apiFetch(`/competitions/${slug}`).then(setData).catch(()=>{}).finally(()=>setLoading(false))},[slug]);if(!fallback)return <Navigate to="/competitions" replace/>;const c=data?.competition||fallback,stories=demoArticles.filter(a=>a.competitionSlug===slug),matches=data?.matches||[],standings=data?.standings||[];return <main className="page content-page"><Link className="back-link" to="/competitions"><ArrowLeft size={15}/> All competitions</Link><section className="competition-hero"><div className="competition-hero-mark">{c.code}</div><div className="competition-hero-copy"><small>{c.scope} · {c.type}</small><h1>{lang==='am'?c.nameAm:c.name}</h1><p>{lang==='am'?c.descriptionAm:c.description}</p></div><div className={`data-pill ${data?.demo?'demo':''}`}><span/>{data?.demo?'Directory mode':'Live competition data'}</div></section><div className="competition-tabs">{[['overview','Overview'],['fixtures','Fixtures'],['table','Table'],['news','News']].map(([id,label])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}>{label}</button>)}</div>{tab==='overview'&&<div className="competition-layout"><section className="surface-card competition-overview"><div className="card-heading"><div><span className="eyebrow">Competition hub</span><h2>{lang==='am'?'ሁሉም ነገር በአንድ ቦታ':'Everything in one match-day hub'}</h2></div><Trophy size={22}/></div><div className="overview-metrics"><Metric icon={<CalendarDays size={18}/>} value={matches.length||'—'} label="Loaded fixtures"/><Metric icon={<Radio size={18}/>} value={matches.filter(isLive).length||'—'} label="Live now"/><Metric icon={<Newspaper size={18}/>} value={stories.length||'—'} label="Desk stories"/></div><p className="muted-copy">{lang==='am'?'ይህ ገጽ በውድድሩ ዙሪያ ጨዋታዎችን፣ ደረጃ ሰንጠረዥን እና ዜናዎችን ያደራጃል።':'This page organizes fixtures, standings and stories around the competition. When the live data source is connected, the content updates automatically.'}</p></section><StoryList stories={stories} lang={lang}/></div>}{tab==='fixtures'&&<section className="surface-card"><div className="card-heading"><div><span className="eyebrow">Fixtures</span><h2>Match schedule</h2></div><Clock3 size={21}/></div><div className="fixture-list">{loading?<div className="empty-panel">Loading competition fixtures…</div>:matches.length?matches.map(m=><Fixture key={m.id} match={m}/>):<div className="empty-panel">No verified fixture feed is connected for this competition yet.</div>}</div></section>}{tab==='table'&&<section className="surface-card"><div className="card-heading"><div><span className="eyebrow">Standings</span><h2>Competition table</h2></div><Trophy size={21}/></div>{standings.length?<div className="full-table"><div className="full-table-row head"><span>#</span><span>Team</span><span>MP</span><span>GD</span><span>PTS</span></div>{standings.map((r,i)=><div className="full-table-row" key={r.team_id||r.name_en}><span>{i+1}</span><b>{r.name_en}</b><span>{r.mp}</span><span>{r.gd}</span><strong>{r.pts}</strong></div>)}</div>:<div className="empty-panel">The table will appear when league-specific results are available.</div>}</section>}{tab==='news'&&<StoryList stories={stories} lang={lang} expanded/>}</main>}
function Metric({icon,value,label}){return <div className="metric-card"><span>{icon}</span><div><strong>{value}</strong><small>{label}</small></div></div>}
function Fixture({match}){const time=isLive(match)?`${match.current_minute||0}′`:isFinished(match)?'FT':new Intl.DateTimeFormat('en-GB',{hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(match.match_date));return <div className="competition-fixture"><span className={isLive(match)?'fixture-live':''}>{time}</span><div><b>{match.home_en}</b><b>{match.away_en}</b></div><div className="fixture-score"><strong>{isLive(match)||isFinished(match)?match.home_score:'—'}</strong><strong>{isLive(match)||isFinished(match)?match.away_score:'—'}</strong></div></div>}
function StoryList({stories,lang,expanded=false}){const list=stories.length?stories:demoArticles.slice(0,expanded?5:3);return <section className="surface-card"><div className="card-heading"><div><span className="eyebrow">News desk</span><h2>{lang==='am'?'የውድድር ታሪኮች':'Competition stories'}</h2></div><Newspaper size={21}/></div><div className="compact-story-list">{list.map(a=><Link to={`/news/${a.slug}`} className="compact-story" key={a.slug}><span className="story-category">{a.category}</span><div><b>{lang==='am'?a.titleAm:a.titleEn}</b><small>{a.demo?'Demo desk · ':''}{new Date(a.publishedAt).toLocaleDateString()}</small></div><ChevronRight size={16}/></Link>)}</div></section>}
