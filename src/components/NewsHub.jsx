import { useEffect,useMemo,useState } from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight,Clock3,Newspaper,Sparkles } from 'lucide-react';
import { apiFetch } from '../api';
import { demoArticles } from '../data/content';
const categories=['All','Transfers','Injuries','Preview','Post-match','Platform'];
export default function NewsHub({lang='en',compact=false}){const[articles,setArticles]=useState(demoArticles);const[category,setCategory]=useState('All');useEffect(()=>{apiFetch('/news').then(d=>Array.isArray(d)&&d.length&&setArticles(d)).catch(()=>{})},[]);const filtered=useMemo(()=>category==='All'?articles:articles.filter(a=>a.category===category),[articles,category]);if(compact){const lead=articles[0]||demoArticles[0];return <section className="home-section news-teaser"><div className="section-title-row"><div><span className="eyebrow">News desk</span><h2>{lang==='am'?'ዜና፣ ዝውውር እና የጨዋታ ቅድመ እይታ':'News, transfers and match context'}</h2></div><Link className="text-link" to="/news">Open news <ArrowRight size={14}/></Link></div><div className="news-teaser-grid"><Link className="lead-story" to={`/news/${lead.slug}`}><div className="story-art"><Newspaper size={30}/><span>ETHIO DESK</span></div><div><span className="story-category">{lead.category}</span><h3>{lang==='am'?lead.titleAm:lead.titleEn}</h3><p>{lang==='am'?lead.summaryAm:lead.summaryEn}</p><small>{lead.demo?'Demo editorial · ':''}{fmt(lead.publishedAt)}</small></div></Link><div className="mini-story-stack">{articles.slice(1,4).map(a=><Link className="mini-story" key={a.slug} to={`/news/${a.slug}`}><span className="story-category">{a.category}</span><b>{lang==='am'?a.titleAm:a.titleEn}</b><small>{fmt(a.publishedAt)}</small></Link>)}</div></div></section>}return <main className="page content-page"><div className="content-hero news-hero"><div><span className="eyebrow">EthioLiveScores newsroom</span><h1>{lang==='am'?'የእግር ኳስ ዜና በእንግሊዝኛ እና በአማርኛ':'Football news built for English and Amharic readers'}</h1><p>{lang==='am'?'ዝውውሮች፣ ጉዳቶች፣ የጨዋታ ቅድመ እይታዎች፣ የድህረ ጨዋታ ሪፖርቶች እና የክለብ ዝመናዎች።':'Transfers, injuries, previews, post-match reports and club updates in one structured publishing layer.'}</p></div><div className="newsroom-badge"><Sparkles size={20}/><b>Bilingual desk</b><small>EN + አማ</small></div></div><div className="news-filters">{categories.map(c=><button key={c} className={category===c?'active':''} onClick={()=>setCategory(c)}>{c}</button>)}</div><div className="news-grid">{filtered.map((a,i)=><Link className={`news-card ${i===0&&category==='All'?'featured':''}`} to={`/news/${a.slug}`} key={a.slug}><div className="news-card-art"><span>{a.category}</span><Newspaper size={i===0?36:25}/></div><div className="news-card-body"><span className="story-category">{a.category}</span><h2>{lang==='am'?a.titleAm:a.titleEn}</h2><p>{lang==='am'?a.summaryAm:a.summaryEn}</p><div className="story-meta"><Clock3 size={13}/>{a.demo&&<span>Demo desk</span>}<span>{fmt(a.publishedAt)}</span></div></div></Link>)}</div></main>}
function fmt(v){try{return new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(v))}catch{return''}}
